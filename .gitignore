# Student progress data — not committed so each student has their own
.learner/active.json
.learner/projects/
.learner/pending-answer.json

# Keep the directory itself
!.learner/.gitkeep

# Extension build artifacts
extension/node_modules/
extension/out/
extension/learner.vsix

# Claude local settings (API key etc.)
.claude/settings.local.json

# Student projects — each gets its own git repo inside here
projects/

# Node
node_modules/

# macOS
.DS_Store
